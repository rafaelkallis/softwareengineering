package we.are.bubblesort.MovieApp.client;

import com.google.gwt.event.shared.EventHandler;

public interface AppActivateSectionEventHandler extends EventHandler {
	void onActivateSection(int sectionId);
}
