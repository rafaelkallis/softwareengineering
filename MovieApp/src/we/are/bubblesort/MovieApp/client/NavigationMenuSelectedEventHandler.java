package we.are.bubblesort.MovieApp.client;

import com.google.gwt.event.shared.EventHandler;

public interface NavigationMenuSelectedEventHandler extends EventHandler {
	void onMenuSelected(Section section);
}
